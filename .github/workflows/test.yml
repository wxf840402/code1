#Author：Eiwen
#Date： 2024/04/16
 
name: 阿飞的workflow
 
on: push
 
jobs:
  my-job:
    name: 测试任务
    runs-on: ubuntu-latest
    steps:
      - name: Set environment variables
        run: |
         echo "LANG=en_US.UTF-8" >> $GITHUB_ENV
         echo "LANGUAGE=en_US.UTF-8" >> $GITHUB_ENV
      - name: 打印文本
        run: |
          echo 我的名字是阿飞
      #- name: 下载源码文件
        #uses: actions/checkout@master 
      #- name: 上传文件到服务器
       # uses: appleboy/scp-action@v0.1.7
       # with:
          #host: ${{ secrets.HOST }}
          #username: ${{ secrets.USERNAME }}
          #password: ${{ secrets.PASSWORD }}
          #port: ${{ secrets.PORT }}
          #source: "*"
          #target: "./website/eiwen-disk"
      - name: 执行远程SSH命令
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 150.242.98.65 #${{ secrets.REMOTE_HOST }}
          username: lenovo #${{ secrets.REMOTE_USER }}
          #password: singer8402 #${{ secrets.PASSWORD }}
          port: 38028 #${{ secrets.PORT }}
          script: | 
           whoami
           ls -al
      

